import { analyticsService } from './analyticsService';

export const providerService = {
  getProviderHealth:  analyticsService.getProviderHealth,
  getActuatorHealth:  analyticsService.getActuatorHealth,
};
