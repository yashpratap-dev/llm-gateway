package dev.yashpratap.llmgateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class reserved for application-wide property concerns.
 *
 * <p>{@code @ConfigurationProperties} records are discovered automatically via
 * {@code @ConfigurationPropertiesScan} on
 * {@link dev.yashpratap.llmgateway.LlmGatewayApplication} and do not need explicit
 * registration here.</p>
 */
@Configuration
public class PropertiesConfig {
}
